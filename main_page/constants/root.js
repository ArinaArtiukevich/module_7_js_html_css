const ROOT_NAVBAR = document.getElementById('navbar');
const ROOT_CATEGORY_MENU = document.getElementById('category_menu');
const ROOT_COUPONS = document.getElementById('coupons');
const ROOT_LIKED_COUPONS = document.getElementById('likedCoupons');
const ROOT_LOGIN = document.getElementById('login');
const ROOT_REGISTRATION = document.getElementById('registration');
const ROOT_ITEM_DETAILS = document.getElementById('item_details');
const ROOT_NEW_ITEM = document.getElementById('new_item');
const ROOT_NEW_TAG = document.getElementById('new_tag');
const ROOT_CHECKOUT = document.getElementById('checkout');
const ROOT_USER_INFO = document.getElementById('user_info');
const ROOT_SPINNER = document.getElementById('spinner');
const ROOT_FOOTER = document.getElementById('footer');
const ROOT_ERROR = document.getElementById('error');

const DEFAULT_VALUE_DROPDOWN = "All categories";

const ROLE_ADMIN = "ROLE_ADMIN";

const scrollTop = "scrollTop";
const scrollTopUnload = "scrollTopUnload";
const changeScrollPosition = "changeScrollPosition";
const isLoading = "isLoading";
